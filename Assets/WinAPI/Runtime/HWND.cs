using System;

namespace work.ctrl3d
{
    public struct HWND
    {
        public static IntPtr BOTTOM = (IntPtr)1;
        public static IntPtr NOTOPMOST = (IntPtr)(-2);
        public static IntPtr TOP = (IntPtr)0;
        public static IntPtr TOPMOST = (IntPtr)(-1);
    }
}